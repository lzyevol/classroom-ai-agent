# Browser-side API adapter: mock or real
NEXT_PUBLIC_API_MODE=mock

# Used by the Next.js /backend rewrite on the host machine.
# In Docker Compose, override this with http://backend:8000.
BACKEND_URL=http://127.0.0.1:8100
