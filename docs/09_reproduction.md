# C 部分复现记录

## 已完成

1. 固定 Node、pnpm、Next.js 和 React 版本，建立最小页面。
2. 建立 QA 请求适配器、Mock/real 开关、引用与错误展示。
3. 实现可处理任意 UTF-8 分片和 CRLF 的课堂 SSE 解析器。
4. 实现课堂消息状态、等待/结束/error/Abort/断流处理。
5. 实现 Mock 练习的创建、作答、提交和批改展示。
6. 增加成功 JSON 与 SSE 的运行时契约校验。
7. 从参考代码核对并恢复首批白板动作安全执行和 actionId 去重。
8. 增加不包含请求正文和凭据的联调诊断。
9. 建立 Vitest 与 Playwright 两层自动化测试。

## 当前集成进展

- A 的 v1.0 数据成品、图谱导入脚本和教材图片已经进入 C 集成分支。
- B 的 QA/knowledge 提交已经合并，A 分支中的后端复现基底补齐了 FastAPI 启动入口与其他业务模块。
- QA 已完成隔离 L2：浏览器经 Next.js rewrite 请求真实 FastAPI 路由，检索/模型边界使用固定替身。
- 数据检索已完成真实 L2：Compose 导入独立 Neo4j 后，经 Next.js 代理命中 A 的真实 Chunk、章节和教材图片；同版本重复初始化会跳过导入。
- 后端 57 项测试、前端 52 项测试、12 项 Mock 浏览器回归和 3 项 L2 浏览器联调均通过。

## 当前简化

- 页面默认仍为 L1 Mock，需显式设置 `NEXT_PUBLIC_API_MODE=real` 才访问后端。
- 课堂没有发送真实 StatelessChatRequest。
- 白板只支持 `wb_open` 和 `wb_draw_text`。
- 练习结果是固定 fixture，不是浏览器判分，也没有数据库记录。
- Mock 模式教材引用仍是明确的占位符；真实检索 L2 已核对稳定 `chunk_id`，但 QA 回答尚未通过真实模型验证。

## 下一次联合推进所需输入

- A/B：在独立 Neo4j 中执行导入并用 B 查询核对真实引用。
- B：课堂合法请求与完整 SSE fixture；当前 B 分支没有课堂服务交付。
- B：可重复生成的固定练习题库 seed；运行库不能作为普通代码合并。
- B：确认模型调用失败应返回 503，而不是 HTTP 200 的“证据不足”对象。
- B/C：共同确认 Action 名称、参数范围和未知字段兼容策略。
